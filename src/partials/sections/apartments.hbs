<section class="section property-block tabs js-fade-in-block js-tabs" id="apartments">
  <div class="container">
    <header class="section__header">
      <h2>Квартиры</h2>
      {{>tabs-slider content=apartments.tabs}}
    </header>
    <div class="tabs__content">
      {{#each apartments.panes }}
      <div class="tabs__pane {{#if @first}}tabs__pane--active{{/if}} js-tabs-pane property-block__wrap js-property-block-slider" data-tab-content="{{ this.id }}">
        <div class="property-block__slider swiper ">
          <div class="swiper-wrapper">
            {{#each this.objects }}
              {{>property-card content=this }}
            {{/each}}
          </div>
        </div>
        {{>slider-navigation}}
      </div>      
      {{/each}}
    </div>
  </div>
</section>
