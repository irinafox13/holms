<div class="form-field js-form-field {{content.disabled}}">
    <input
        name="{{content.name}}"
        type="text"
        {{#if content.required}}required{{/if}}
        class="form-field__input js-form-field-input {{content.dopClass}}"
        {{content.disabled}}
            {{#if content.pattern}}pattern="{{content.pattern}}"{{/if}}
        value="{{content.value}}"
    >
    <label class="form-field__label">
        {{content.title}}{{#if content.required}}*{{/if}}
    </label>
    <div class="form-field__caption form-field__caption--error">
        {{content.error}}
    </div>
</div>
